{
  "name": "shorturl-worker",
  "version": "1.0.0",
  "description": "Short URL service using Cloudflare Workers + D1 (KV kept read-only for legacy codes)",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:query": "wrangler d1 execute k-rider-signups --remote --command"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
