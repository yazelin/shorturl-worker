{
  "name": "shorturl-worker",
  "version": "1.0.0",
  "description": "Short URL service using Cloudflare Workers + KV",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "kv:create": "wrangler kv:namespace create URLS",
    "kv:list": "wrangler kv:key list --binding=URLS"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
